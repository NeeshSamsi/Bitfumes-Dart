main() {
  String name = "Neesh Samsi";

  // print(name.toUpperCase());
  // print(name.toLowerCase());

  // print(name.length);
  // print(name.trim());
  // print(name.trim().length);

  // print(name.replaceAll('Neesh', 'Shrav'));

  // print(name.codeUnits); // Returns array of ASCII code for each letter

  // print(name.isEmpty);
  // print(name.isNotEmpty);
}
